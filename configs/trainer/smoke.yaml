accelerator: auto
precision: 32-true
max_epochs: 1
log_every_n_steps: 1
limit_train_batches: 5
limit_val_batches: 2
gradient_clip_val: null
deterministic: true

early_stopping: false
es_monitor: val/loss
es_mode: min
es_patience: 10
es_min_delta: 0.0
